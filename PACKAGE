load("@prelude//rust:cargo_package.bzl", "DEFAULT_REINDEER_PLATFORMS", "set_reindeer_platforms")

set_reindeer_platforms(
    select({
        "DEFAULT": DEFAULT_REINDEER_PLATFORMS,
        "prelude//os:windows": "windows-x86_64",
    }),
)
